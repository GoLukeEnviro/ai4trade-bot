global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "ai4trade-bot"
    static_configs:
      - targets: ["bot:9090"]

  - job_name: "rainbow-engine"
    static_configs:
      - targets: ["rainbow:8000"]
    metrics_path: "/metrics/prometheus"

rule_files:
  - "alertmanager_rules.yml"
